-- Creates the SRCalc schema.
--
-- For now, this is HSQLDB. We'll find out what our actual database is later.
CREATE TABLE SPECIALTY(ID INTEGER NOT NULL PRIMARY KEY, VISTA_ID INTEGER NOT NULL, NAME VARCHAR(255));

